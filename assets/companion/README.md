# Companion asset slots

Optional companion images can be placed here as `idle.png`, `thinking.png`,
`working.png`, `waiting.png`, `completed.png`, and `error.png`. The TUI keeps a
Unicode `eulr ✦` fallback when the file or terminal image protocol is
unavailable.
